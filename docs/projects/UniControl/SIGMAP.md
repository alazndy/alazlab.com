<!-- Generated by SigMap gen-context.js v8.18.0 -->
<!-- Updated: 2026-07-17T13:45:37.848Z -->
<!-- sigmap: version=8.18.0 -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures
## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| To rank files by topic | `sigmap --query "<topic>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

Always run `sigmap ask` (or `sigmap --query`) before searching for files relevant to a task.

## src

### src/hal.c
```
hal_init_gpios() → void
hal_init_twai() → void
hal_init_uart() → void
hal_init_i2c() → void
hal_init_sd() → void
hal_init() → void
get_rtc_time(char* time_str, int max_len) → void
log_alarm_to_sd() → void
set_output(int mos_idx, int rel_idx, bool state) → void
get_input(int in_idx) → int
```

### src/logic.c
```
update_vehicle_display(float value) → void
task_radar(void *pvParameters) → void
while(1)
if(settings.sensor_mode == 1 || settings.sensor_mode == 2)
if(!rx_msg.extd)
if(rx_msg.identifier == 0x310 || rx_msg.identifier == 0x320)
if(settings.dbg_can)
if(settings.dbg_can)
if(min_dist < settings.danger_zone)
task_nextion(void *pvParameters) → void
while(1)
if(rxBytes > 0)
for(int i = 0; i < rxBytes; i++)
if(data[i] == 0x00 || data[i] == 0xFF)
if(settings.dbg_nextion)
task_buzzer(void *pvParameters) → void
while(1)
if(buzzerActive && settings.audio_alarm)
task_io(void *pvParameters) → void
while(1)
if(settings.io_link_1)
if(in1 == 0)
task_sys(void *pvParameters) → void
while(1)
logic_init() → void
```

### src/main.c
```
app_main() → void
```

### src/settings.c
```
set_default_settings() → void
init_settings() → void
if(err == ESP_ERR_NVS_NO_FREE_PAGES || err == ESP_ERR_NVS_NEW_VERSION_FOUND)
load_settings() → void
if(err != ESP_OK)
if(err != ESP_OK)
save_settings() → void
if(err == ESP_OK)
if(err == ESP_OK)
```

### src/web.c
```
get_handler(httpd_req_t *req) → static esp_err_t
save_post_handler(httpd_req_t *req) → static esp_err_t
tog_handler(httpd_req_t *req) → static esp_err_t
if(m_idx >= 1 && m_idx <= 3)
if(r_idx >= 1 && r_idx <= 3)
dl_handler(httpd_req_t *req) → static esp_err_t
if(!f)
ota_post_handler(httpd_req_t *req) → static esp_err_t
if(!update_partition)
while(remaining > 0)
web_init() → void
```
